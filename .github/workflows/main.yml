name: Test secrets

on:
  push:
    branches:
      - master    # если у тебя главная ветка называется иначе (master), поменяй здесь

jobs:
  check-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: “Echo POSTGRES_USER”
        run: echo "POSTGRES_USER is '${{ secrets.POSTGRES_USER }}'"

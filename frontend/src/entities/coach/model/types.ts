export type Coach = {
  id: string;
  name: string;
  avatar: string;
  avatarHint: string;
  specialization: string;
  description: string;
  tags: string[];
  rating: number;
  price: string;
  profileUrl: string;
};

import { TeamManagementPage } from "@/views/team-management";

// This is now a dynamic route, although the page content is still static.
// A future step would be to fetch team data and pass it as props.
export default TeamManagementPage;

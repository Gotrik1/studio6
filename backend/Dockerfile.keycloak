FROM quay.io/keycloak/keycloak:26.3.1

# This build step is performed only once when the image is built.
RUN /opt/keycloak/bin/kc.sh build --db=postgres

# The CMD is now handled by docker-compose.yml to pass runtime arguments correctly.
# No CMD here allows for more flexible runtime configuration.

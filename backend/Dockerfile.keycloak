# Используем официальный образ Keycloak как базовый
FROM quay.io/keycloak/keycloak:25.0.1

# Переключаемся на пользователя root для установки пакетов
USER root

# Устанавливаем curl, так как он отсутствует в минималистичном образе,
# и очищаем кэш, чтобы уменьшить размер слоя.
RUN microdnf install -y curl && microdnf clean all

# Возвращаемся к пользователю без root-прав для безопасности
USER keycloak

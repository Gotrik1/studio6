# Main Application Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=prodvor

# pgAdmin (optional)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# Keycloak Bootstrap Admin
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin

# Keycloak Database
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=keycloak_password
KC_DB_DATABASE=keycloak

# Kong API Gateway
KONG_ADMIN_LISTEN=0.0.0.0:8001
KONG_PROXY_LISTEN=0.0.0.0:8000

# Kong Database
KONG_DB_USER=kong
KONG_DB_PASSWORD=kong_password
KONG_DB_DATABASE=kong

# RabbitMQ
RABBITMQ_DEFAULT_USER=user
RABBITMQ_DEFAULT_PASSWORD=password

# Application URLs & Connection Strings
# These are used by the backend and frontend services to connect to each other.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public
REDIS_URL=redis://redis:6379
KAFKA_BROKERS=kafka:9092
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=prodvor
KEYCLOAK_CLIENT_ID=prodvor-frontend
RABBITMQ_URL=amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASSWORD}@rabbitmq:5672
NEXT_PUBLIC_BACKEND_URL=http://kong:8000

# This is used by the prisma client in CI/local scripts
PGPASSWORD=${POSTGRES_PASSWORD}
